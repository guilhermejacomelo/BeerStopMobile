package com.example.beerstopmobile;import android.content.Context;import android.database.sqlite.SQLiteDatabase;import android.database.sqlite.SQLiteOpenHelper;import android.view.View;public class BDBeerStop extends SQLiteOpenHelper{    private static final String DATABASE_NAME = "seu_banco_de_dados.db";    private static final int DATABASE_VERSION = 1;    public BDBeerStop(Context context) {        super(context, DATABASE_NAME, null, DATABASE_VERSION);    }    @Override    public void onCreate(SQLiteDatabase db) {        String createTableQuery = "CREATE TABLE clientes (" +                "id INTEGER PRIMARY KEY AUTOINCREMENT," +                "nome TEXT," +                "cpf TEXT," +                "telefone TEXT)";        db.execSQL(createTableQuery);    }    @Override    public void onUpgrade(SQLiteDatabase db, int oldVersion, int newVersion) {        // Atualização do banco de dados (se necessário)        // Aqui você pode adicionar lógica para migrar os dados de versões anteriores, se necessário        // Para este exemplo, vamos descartar a tabela e criar novamente        db.execSQL("DROP TABLE IF EXISTS clientes");        onCreate(db);    }}